# Face ID for Chrome Extension

### Installation

```
brew install cmake
pip install -r requirements.txt
```

Takes a reference picture if not already in path, then verifies face in real-time compared to reference picture.